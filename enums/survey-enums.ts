export enum QuestionType {
  //! Make sure these match with the backend
  doubleInput = 0,
  doubleInputOrAvg = 1,
  intInput = 2,
  intInputOrAvg = 3,
  textInput = 4,
  percentage = 5,
  percentageOrAvg = 6,
  toggle = 7,
  select = 8,
  date = 9,
  frequency = 10,
}
